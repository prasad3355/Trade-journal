import { formatCurrency, formatNumber, formatSigned } from '../../utils/formatters'
function PerformanceTable({ title, rows, onSelect }) { return <section className="analysis-panel analysis-table"><div className="analysis-panel__heading"><h2>{title}</h2><span>{rows.length} groups</span></div><div className="analysis-table__head"><span>Group</span><span>Trades</span><span>Win rate</span><span>Net P&amp;L</span><span>Avg P&amp;L</span></div>{rows.map((row) => <button type="button" key={row.label} className="analysis-table__row" onClick={() => onSelect?.(row.label)}><strong>{row.label}</strong><span>{row.trades ?? row.wins + row.losses + row.breakEven}</span><span>{Math.round(row.winRate * 100)}%</span><b className={row.netPnl >= 0 ? 'profit' : 'loss'}>{formatSigned(formatCurrency(row.netPnl))}</b><span>{formatSigned(formatCurrency(row.averagePnl))}</span></button>)}</section> }
export default PerformanceTable
